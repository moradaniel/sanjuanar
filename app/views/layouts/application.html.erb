<!DOCTYPE html>
<html>
<head>
  <title>
    <% if content_for?(:title) %>
          <%= yield :title %>

    <% else %>
        San Juan AR
    <% end %>


  </title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<% flash.each do |key, value| %>
             <div class='flash' id="<%= key %>">
               <%= value %>

             </div>
<% end %>

<h1><%= link_to "SanJuanAR", root_path%></h1>
<%# This line indicates to the layout where the current action’s template is to be rendered %>
<nav>
  <%= link_to "Sign up", new_user_registration_path %>
</nav>
<%= yield %>

</body>
</html>
